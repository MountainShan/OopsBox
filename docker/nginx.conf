user www-data;
worker_processes auto;
pid /run/nginx.pid;

events { worker_connections 1024; }

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    sendfile on;
    keepalive_timeout 65;
    client_max_body_size 100m;

    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      close;
    }

    server {
        listen 80 default_server;
        server_name _;

        # Auth verification (internal)
        location = /api/auth/verify {
            internal;
            proxy_pass http://127.0.0.1:5000/api/auth/verify;
            proxy_pass_request_body off;
            proxy_set_header Content-Length "";
            proxy_set_header Cookie $http_cookie;
        }

        # Public: login + auth endpoints + static assets
        location = /login {
            proxy_pass http://127.0.0.1:5000;
            proxy_set_header Host $host;
        }
        location /api/auth/ {
            proxy_pass http://127.0.0.1:5000;
            proxy_set_header Host $host;
        }
        location /static/ {
            proxy_pass http://127.0.0.1:5000;
            proxy_set_header Host $host;
        }

        # Protected: all other pages and API
        location / {
            auth_request /api/auth/verify;
            error_page 401 = @login_redirect;
            proxy_pass http://127.0.0.1:5000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_read_timeout 60s;
        }

        location @login_redirect {
            return 302 /login;
        }

        # Dynamic per-project terminal locations (auth_request included per-block)
        include /etc/nginx/conf.d/oopsbox-projects.conf;
    }
}
